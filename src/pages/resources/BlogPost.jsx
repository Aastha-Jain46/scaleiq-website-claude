import ContentTemplate from '../../components/templates/ContentTemplate';

export default function BlogPost() {
  return (
    <ContentTemplate
      eyebrow="Blog · Rajeev Sonthalia"
      title="Why Digital Transformation Fails to Deliver Expected Outcomes in Complex Operations"
    >
      <div className="content-note">
        <strong>[NEEDS INPUT]</strong> The content doc confirms this post is "complete and stays as is," but its body text wasn't included in the file provided — only the title and author. The full article text is needed to publish this page rather than a stub.
      </div>
    </ContentTemplate>
  );
}
